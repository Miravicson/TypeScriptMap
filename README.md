# TypeScript Ft Google Maps API

A project to put TypeScript to use in the real world. 

## Features

* Google Maps JavaScript API
* Implementation of TypeScript interface.
* Uses parce-bundler to transpile and bundle TypeScript.